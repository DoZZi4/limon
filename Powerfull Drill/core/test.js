function isChestWasGenerated() { // true или false
    const random = Math.round(); // 0.01 -> 0.99
    const chest = 0.08; // 1.00 -> 100%, 0.08 -> 8%
    if (random>chest) {
        
    }
}

for (let i = 0; i < 100; i++) {
    console.log(isChestWasGenerated());
}
